
  <%= link_to sanitize("&larr; Back to cart"), @cart %>
  <div id="orders">
  <% if @orders.present? %>
    <% @orders.each do |order| %>
      <%= render order %>
    <% end %>
  <% else %>
  <div  class="card mb-2 mt-2">
  <div class="card-body p-2">
    <div class="row d-flex justify-content-between align-items-center">
      <h5 class="mb-0">No order!</h5>
    </div>
  </div>
</div>
  <% end %>
  </div>
